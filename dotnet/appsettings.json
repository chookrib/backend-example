{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "AllowedHosts": "*",
    "ConnectionStrings": {
        "SQLite": "Data Source=db.db;"
    },
  "App": {
    "Name": "backend-example-dotnet",
    "Env": "dev",
    "//LockService": "semaphore[default] | redis",
    "//LockRedisUrl": "redis://:password@localhost:6379/0",
    "JwtExpires": "30d",
    "JwtSecret": "jwt-secret-must-long-than-32-str"
  }
}
